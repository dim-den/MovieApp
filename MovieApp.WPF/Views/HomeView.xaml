<UserControl
    x:Class="MovieApp.WPF.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:MovieApp.WPF.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MovieApp.WPF.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.8*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <controls:MovieCarousel
            Grid.Row="0"
            Grid.Column="0"
            Margin="10,20"
            DataContext="{Binding MovieCarouselViewModel}" />

        <controls:ActorsSummary
            Grid.Row="0"
            Grid.RowSpan="2"
            Grid.Column="1"
            Margin="10 20 20 20"
            VerticalAlignment="Top"
            DataContext="{Binding ActorsSummaryViewModel}" />

    </Grid>
</UserControl>
