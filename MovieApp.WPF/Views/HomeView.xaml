<UserControl x:Class="MovieApp.WPF.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MovieApp.WPF.Views"
             xmlns:controls="clr-namespace:MovieApp.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.8*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <controls:MovieCarousel Grid.Column="0" Grid.Row="0" DataContext="{Binding MovieCarouselViewModel}" Margin="10" />

        <controls:ActorsSummary Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" DataContext="{Binding ActorsSummaryViewModel}" Margin="10" VerticalAlignment="Top" />

    </Grid>
</UserControl>
