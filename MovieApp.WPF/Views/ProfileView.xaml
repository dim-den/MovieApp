<UserControl x:Class="MovieApp.WPF.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MovieApp.WPF.Views" 
             xmlns:converters="clr-namespace:MovieApp.WPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:IsNullConverter x:Key="IsNullConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Column="0" Grid.Row="0" x:Name="imageGrid">
            <Border Margin="20" BorderThickness="3" BorderBrush="{StaticResource BrushPrimary3}" CornerRadius="400">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="Black" Opacity="0.4" RenderingBias="Quality" />
                </Border.Effect>
            <Image Height="200" Width="200" Stretch="Fill" VerticalAlignment="Top">
                <Image.Style>
                    <Style TargetType="Image">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ImageData, Converter={StaticResource IsNullConverter}}" Value="True">
                                <Setter Property="Source" Value="../Resources/default-avatar.png" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ImageData, Converter={StaticResource IsNullConverter}}" Value="False">
                                <Setter Property="Source" Value="{Binding ImageData, TargetNullValue={x:Null}}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
                <Image.Clip>
                    <EllipseGeometry Center="100,100" RadiusX="100" RadiusY="100" />
                </Image.Clip>
            </Image>
            </Border>
            <Button Command="{Binding ChangeImageCommand}" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="20" Content="edit" Width="60">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
                        <Setter Property="Visibility" Value="Hidden"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=imageGrid, Path=IsMouseOver}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <Grid Grid.Column="1" Grid.Row="0">
            <Border  Background="{StaticResource BrushPrimary1}" Margin="50" CornerRadius="20" BorderThickness="2">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.2" Color="Black"/>
                </Border.Effect>
            <StackPanel Orientation="Vertical" >
                <TextBlock Text="{Binding Username}" Foreground="{StaticResource BrushPrimary2}" FontSize="32" FontWeight="Bold" Margin="40 20 0 0" />
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Name}" Foreground="{StaticResource BrushPrimary2}" FontSize="24" FontStyle="Italic" Margin="20 0 10 0"/>
                    <TextBlock Text="{Binding Surname}" Foreground="{StaticResource BrushPrimary2}" FontSize="24" FontStyle="Italic" Margin="0"/>
                </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Films watched: " Foreground="{StaticResource BrushPrimary2}" FontSize="24" FontStyle="Italic" Margin="20 0 10 0"/>
                        <TextBlock Text="{Binding FilmsWatched}" Foreground="{StaticResource BrushPrimary2}" FontSize="24" FontStyle="Italic" Margin="0"/>
                    </StackPanel>

                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
