<UserControl x:Class="MovieApp.WPF.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:MovieApp.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Rectangle>
            <Rectangle.Fill>
                <LinearGradientBrush EndPoint="0.1,1" StartPoint="0.8,0">
                    <GradientStop Color="{StaticResource ColorPrimary1}"/>
                    <GradientStop Color="{StaticResource ColorPrimary2}" Offset="1"/>
                    <GradientStop Color="{StaticResource ColorPrimary3}" Offset="0.7"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <Border Margin="15" Padding="5"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Background="White"
        BorderBrush="{StaticResource BrushPrimary1}"
        BorderThickness="2"
        CornerRadius="16">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Color="Gray" />
            </Border.Effect>
            
            <Grid VerticalAlignment="Center" Margin="10 0">
                <TextBlock Text="Registration" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="5" Foreground="Gray" FontSize="24"/>
                <StackPanel VerticalAlignment="Center" Margin="15 30 15 0" HorizontalAlignment="Center" Orientation="Vertical">
                    <TextBox Margin="0 5 0 0" materialDesign:HintAssist.Hint="username" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="250" FontFamily="Champagne &amp; Limousines" FontSize="18"/>
                    <TextBox Margin="0 5 0 0" materialDesign:HintAssist.Hint="email" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="250" FontFamily="Champagne &amp; Limousines" FontSize="18"/>
                    <TextBox Margin="0 5 0 0" materialDesign:HintAssist.Hint="name" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="250" FontFamily="Champagne &amp; Limousines" FontSize="18"/>
                    <TextBox Margin="0 5 0 0" materialDesign:HintAssist.Hint="surname" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="250" FontFamily="Champagne &amp; Limousines" FontSize="18"/>
                    <PasswordBox Margin="0 5 0 0" materialDesign:HintAssist.Hint="password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Width="250" FontFamily="Champagne &amp; Limousines" FontSize="18"/>
                    <PasswordBox Margin="0 5 0 0" materialDesign:HintAssist.Hint="confirm password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Width="250" FontFamily="Champagne &amp; Limousines" FontSize="18"/>
                    <Button Width="150" HorizontalAlignment="Center" VerticalContentAlignment="Center" Background="{StaticResource BrushPrimary1}" Margin="0 20 0 5" Content="Register"/>
                    <Button Content="Already have an account?" Command="{Binding ChangeViewCommand}" CommandParameter="ToLogin"
                            Style="{StaticResource MaterialDesignOutlinedButton}" BorderThickness="0" Margin="0 5" HorizontalAlignment="Center" Foreground="{StaticResource BrushPrimary1}"/>
                </StackPanel>

            </Grid>
        </Border>
    </Grid>
</UserControl>
