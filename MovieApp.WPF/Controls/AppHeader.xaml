<UserControl x:Class="MovieApp.WPF.Controls.AppHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MovieApp.WPF.Controls" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:MovieApp.WPF.Converters"
             mc:Ignorable="d" 
             d:DesignWidth="800">
    <UserControl.Resources>
        <converters:IsNullConverter x:Key="IsNullConverter" />
    </UserControl.Resources>
    <Grid Background="{StaticResource BrushPrimary2}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <materialDesign:PackIcon Grid.Column="0" Kind="Home" Width="48" Height="48" Margin="5" Foreground="White"/>
        
        <TextBox Grid.Column="1" Style="{StaticResource MaterialDesignTextBox}" Margin="20 10" Padding="5" FontSize="18" Background="White"/>

        <Grid Grid.Column="2">
            <StackPanel Orientation="Horizontal">
                <Image Width="32" Height="32" >
                    
                    <Image.Style>
                        <Style TargetType="Image">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ImageData}" Value="{x:Null}">
                                    <Setter Property="Source" Value="../Resources/default-avatar.png" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ImageData, Converter={StaticResource IsNullConverter}}" Value="False">
                                    <Setter Property="Source" Value="{Binding ImageData}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>
                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Margin="5 5 10 5" FontSize="18"/>               
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
